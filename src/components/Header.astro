---
const { lang = 'cs' } = Astro.props;

const translations = {
  cs: {
    services: 'Služby',
    about: 'O nás',
    blog: 'Blog',
    contact: 'Kontakt',
  },
  en: {
    services: 'Services',
    about: 'About Us',
    blog: 'Blog',
    contact: 'Contact',
  },
  de: {
    services: 'Dienstleistungen',
    about: 'Über uns',
    blog: 'Blog',
    contact: 'Kontakt',
  },
  es: {
    services: 'Servicios',
    about: 'Sobre nosotros',
    blog: 'Blog',
    contact: 'Contacto',
  },
  ru: {
    services: 'Услуги',
    about: 'О нас',
    blog: 'Блог',
    contact: 'Контакты',
  },
};

const languages = [
    { code: 'cs', label: 'CZ', flag: '🇨🇿' },
    { code: 'en', label: 'EN', flag: '🇺🇸' },
    { code: 'de', label: 'DE', flag: '🇩🇪' },
    { code: 'ru', label: 'RU', flag: '🇷🇺' },
    { code: 'es', label: 'ES', flag: '🇵🇦' }
];

const currentLangObj = languages.find(l => l.code === lang) || languages[0];
const t = translations[lang] || translations.cs;

// Helper for localized internal links
const getLink = (path) => {
    if (path === '/') return lang === 'cs' ? '/' : `/${lang}/`;
    if (path.startsWith('#')) return lang === 'cs' ? `/${path}` : `/${lang}/${path}`;
    return lang === 'cs' ? path : `/${lang}${path}`;
};

const currentPath = Astro.url.pathname;
---

<header class="header">
  <div class="container header-content">
    <a href={getLink('/')} class="logo">
      <img src="/images/Transparent Logo.svg" alt="Progressivo Logo" class="logo-light" width="180" height="120" />
      <img src="/images/Transparent-Logo-tmave.png" alt="Progressivo Logo" class="logo-dark" width="180" height="120" />
    </a>

    <!-- Navigation Removed - Moved to Floating Dock -->

    <div class="header-actions">
        <!-- Language Switcher Dropdown -->
        <div class="lang-switcher-dropdown">
            <button class="lang-btn" aria-label="Select Language" aria-expanded="false">
                <span class="flag">{currentLangObj.flag}</span>
                <span class="code">{currentLangObj.label}</span>
                <svg class="chevron" xmlns="http://www.w3.org/2000/svg" width="16" height="16" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2" stroke-linecap="round" stroke-linejoin="round"><path d="m6 9 6 6 6-6"/></svg>
            </button>
            <div class="lang-menu">
                {languages.map(l => {
                    // Logic to preserve current path but change language
                    let targetPath = '/';
                    const segments = currentPath.split('/').filter(Boolean); // e.g. ['es', 'o-nas'] or ['o-nas']

                    // Identify if the first segment is a language code
                    const firstSegmentIsLang = languages.some(lang => lang.code === segments[0]);

                    if (l.code === 'cs') {
                         // Switching TO Czech (root)
                         if (firstSegmentIsLang) {
                             // Remove the lang prefix
                             const newSegments = segments.slice(1);
                             targetPath = '/' + newSegments.join('/');
                         } else {
                             // Already at root or some deep path in CS
                             targetPath = currentPath;
                         }
                    } else {
                        // Switching TO another language (e.g. 'en')
                        if (firstSegmentIsLang) {
                            // Replace the existing lang prefix
                            const newSegments = [l.code, ...segments.slice(1)];
                            targetPath = '/' + newSegments.join('/');
                        } else {
                            // Add the new lang prefix
                            const newSegments = [l.code, ...segments];
                            targetPath = '/' + newSegments.join('/');
                        }
                    }

                    // Fix root path edge cases
                    if (targetPath === '') targetPath = '/';
                    else if (targetPath === `/${l.code}`) targetPath = `/${l.code}/`;
                    else if (targetPath === '//') targetPath = '/';

                    return (
                        <a href={targetPath} class={`lang-item ${l.code === lang ? 'active' : ''}`} data-lang={l.code}>
                            <span class="flag">{l.flag}</span>
                            <span class="label">{l.label}</span>
                        </a>
                    )
                })}
            </div>
        </div>

        <button id="theme-toggle" aria-label="Toggle Dark Mode">
            <svg class="sun-icon" xmlns="http://www.w3.org/2000/svg" width="20" height="20" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2" stroke-linecap="round" stroke-linejoin="round"><circle cx="12" cy="12" r="4"/><path d="M12 2v2"/><path d="M12 20v2"/><path d="m4.93 4.93 1.41 1.41"/><path d="m17.66 17.66 1.41 1.41"/><path d="M2 12h2"/><path d="M20 12h2"/><path d="m6.34 17.66-1.41 1.41"/><path d="m19.07 4.93-1.41 1.41"/></svg>
            <svg class="moon-icon" xmlns="http://www.w3.org/2000/svg" width="20" height="20" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2" stroke-linecap="round" stroke-linejoin="round"><path d="M12 3a6 6 0 0 0 9 9 9 9 0 1 1-9-9Z"/></svg>
        </button>
    </div>
  </div>
</header>

<script>
    function initHeader() {
        // Theme Toggle Logic
        const themeToggle = document.getElementById('theme-toggle');
        const root = document.documentElement;

        // Check for saved user preference, if any, on load
        const savedTheme = localStorage.getItem('theme');
        if (savedTheme) {
            root.classList.toggle('dark', savedTheme === 'dark');
        } else {
            // Check system preference
            const systemPrefersDark = window.matchMedia('(prefers-color-scheme: dark)').matches;
            root.classList.toggle('dark', systemPrefersDark);
        }

        if (themeToggle) {
             // Remove old listener if exists (good practice for SPA navigations, though strictly replaced elements don't need it)
             const newThemeToggle = themeToggle.cloneNode(true);
             themeToggle.parentNode.replaceChild(newThemeToggle, themeToggle);

             newThemeToggle.addEventListener('click', () => {
                root.classList.toggle('dark');
                localStorage.setItem('theme', root.classList.contains('dark') ? 'dark' : 'light');
            });
        }

        // Language Switcher Dropdown Logic
        const langBtn = document.querySelector('.lang-btn');
        const langMenu = document.querySelector('.lang-menu');
        const langSwitcher = document.querySelector('.lang-switcher-dropdown');

        if (langBtn && langMenu && langSwitcher) {
            // Reset state
             langMenu.classList.remove('show');
             langBtn.setAttribute('aria-expanded', 'false');

             const newLangBtn = langBtn.cloneNode(true);
             langBtn.parentNode.replaceChild(newLangBtn, langBtn);

            newLangBtn.addEventListener('click', (e) => {
                e.stopPropagation();
                const expanded = newLangBtn.getAttribute('aria-expanded') === 'true';
                newLangBtn.setAttribute('aria-expanded', !expanded);
                langMenu.classList.toggle('show');
            });

            // Close dropdown when clicking outside
            const closeDropdown = (e) => {
                if (!langSwitcher.contains(e.target)) {
                    langMenu.classList.remove('show');
                    newLangBtn.setAttribute('aria-expanded', 'false');
                }
            };
            document.removeEventListener('click', window._closeDropdownHandler); // Cleanup previous
            window._closeDropdownHandler = closeDropdown;
            document.addEventListener('click', closeDropdown);
        }

        // Save language preference on click
        document.querySelectorAll('.lang-item').forEach(item => {
            item.addEventListener('click', () => {
                 const selectedLang = item.getAttribute('data-lang');
                 localStorage.setItem('preferred-lang', selectedLang);
            });
        });
    }

    // Run on initial load
    document.addEventListener('astro:page-load', initHeader);
</script>

<style>
  .header {
    position: fixed; /* Fixed to overlay hero */
    top: 0;
    width: 100%;
    z-index: 90; /* Below Dock (100) */
    background: transparent; /* Transparent utility bar */
    border-bottom: none;
    transition: opacity 0.3s ease, transform 0.3s ease, top 0.3s ease;
    padding-top: max(1rem, env(safe-area-inset-top)); /* Respect iOS safe area */
  }

  /* Hide header when scrolled down */
  :global(.scrolled-down) .header {
      transform: translateY(-100%);
      opacity: 0;
      pointer-events: none;
  }

  .header-content {
    display: flex;
    justify-content: space-between;
    align-items: center;
    /* Removed fixed height: 80px; to allow logo to breathe */
    min-height: 60px;
    padding-bottom: 0.5rem;
  }

  .logo {
      display: block;
      width: auto;
      height: 60px; /* mobil */
      transition: all 0.3s ease;
      opacity: 1; /* Full ink visibility */
  }

  @media (min-width: 768px) {
      .logo {
          height: 90px; /* tablet - proportional increase */
      }
  }

  @media (min-width: 1024px) {
      .logo {
          height: 120px; /* desktop - Masthead size */
      }
  }

  .logo img {
      display: block;
      height: 100%;
      width: auto;
      border-radius: 0;
      box-shadow: none;
      background: transparent;
  }

  .logo-dark {
      display: none !important;
  }

  :global(.dark) .logo-light {
      display: none !important;
  }

  :global(.dark) .logo-dark {
      display: block !important;
  }

  .header-actions {
      display: flex;
      align-items: center;
      gap: 1rem;
  }

  /* Language Switcher */
  .lang-switcher-dropdown {
      position: relative;
  }

  .lang-btn {
      display: flex;
      align-items: center;
      gap: 0.5rem;
      background: rgba(255, 255, 255, 0.3); /* More transparent */
      backdrop-filter: blur(4px);
      border: 1px solid rgba(255,255,255,0.2);
      padding: 0.5rem 0.75rem;
      border-radius: 20px;
      cursor: pointer;
      color: var(--color-text);
      font-weight: 600;
      transition: all 0.2s ease;
      font-family: var(--font-body);
  }

  :global(.dark) .lang-btn {
      background: rgba(30, 41, 59, 0.3);
      border-color: rgba(255,255,255,0.1);
  }

  .lang-btn:hover {
      background: rgba(255, 255, 255, 0.5);
      border-color: var(--color-accent);
  }

  .lang-btn .flag {
      font-size: 1.2rem;
  }

  .lang-btn .chevron {
      width: 14px;
      height: 14px;
      opacity: 0.7;
  }

  .lang-menu {
      position: absolute;
      top: calc(100% + 0.5rem);
      right: 0;
      background: rgba(255, 255, 255, 0.9);
      backdrop-filter: blur(12px);
      -webkit-backdrop-filter: blur(12px);
      border: 1px solid var(--color-border);
      border-radius: 12px;
      padding: 0.5rem;
      min-width: 140px;
      box-shadow: var(--shadow-lg);
      opacity: 0;
      visibility: hidden;
      transform: translateY(-10px);
      transition: all 0.2s ease;
      display: flex;
      flex-direction: column;
      gap: 0.25rem;
  }

  :global(.dark) .lang-menu {
      background: rgba(15, 23, 42, 0.9);
  }

  .lang-menu.show {
      opacity: 1;
      visibility: visible;
      transform: translateY(0);
  }

  .lang-item {
      display: flex;
      align-items: center;
      gap: 0.75rem;
      padding: 0.5rem 1rem;
      text-decoration: none;
      color: var(--color-text);
      border-radius: 8px;
      transition: background 0.2s;
      font-weight: 500;
  }

  .lang-item:hover {
      background: var(--color-bg-alt);
      color: var(--color-accent);
  }

  .lang-item.active {
      background: var(--color-bg-alt);
      color: var(--color-accent);
      font-weight: 700;
  }

  #theme-toggle {
      background: rgba(255,255,255,0.3);
      backdrop-filter: blur(4px);
      border: 1px solid rgba(255,255,255,0.2);
      cursor: pointer;
      color: var(--color-text);
      padding: 0.5rem;
      display: flex;
      align-items: center;
      justify-content: center;
      border-radius: 50%;
      transition: background-color 0.2s ease;
  }

  :global(.dark) #theme-toggle {
      background: rgba(30, 41, 59, 0.3);
      border-color: rgba(255,255,255,0.1);
  }

  #theme-toggle:hover {
      background-color: rgba(255,255,255,0.5);
      border-color: var(--color-accent);
  }

  .sun-icon { display: none; }
  .moon-icon { display: block; }

  :global(.dark) .sun-icon { display: block; }
  :global(.dark) .moon-icon { display: none; }

</style>
